{% extends 'base.html.twig' %}

{% block title %}AdminTech{% endblock %}

{% block body %}
    <h1>AdminTech</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_tech.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ admin_tech.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ admin_tech.email }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ admin_tech.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_tech_index') }}">back to list</a>

    <a href="{{ path('app_admin_tech_edit', {'id': admin_tech.id}) }}">edit</a>

    {{ include('admin_tech/_delete_form.html.twig') }}
{% endblock %}
