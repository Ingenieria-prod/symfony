<form method="post" action="{{ path('app_mufas_delete', {'id': mufa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mufa.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
